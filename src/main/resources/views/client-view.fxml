<?xml version="1.0" encoding="UTF-8"?>
<?import java.net.*?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.control.TableView?>
<?import javafx.scene.control.TableColumn?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.Separator?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.ScrollPane?>

<ScrollPane xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.example.rendezvousmanager.controllers.ClientController" fitToWidth="true">
    <stylesheets>
        <URL value="@../styles.css" />
    </stylesheets>
    
    <VBox spacing="15" style="-fx-padding: 20px;">
        <HBox alignment="TOP_RIGHT" style="-fx-padding: 10px;">
            <Button text="🏠 Accueil" onAction="#goHome" styleClass="btn-home" />
        </HBox>

        <VBox spacing="20" style="-fx-background-color: #f8f9fa; -fx-padding: 20px; -fx-background-radius: 10px;">
            <Label text="👥 Gestion des Clients" styleClass="label-title" />
            <Label text="Gérez votre base de clients" style="-fx-text-fill: #666; -fx-font-size: 14px;" />
        </VBox>

        <VBox spacing="15" style="-fx-background-color: white; -fx-padding: 20px; -fx-background-radius: 10px; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.1), 5, 0, 0, 2);">
            <Label text="📋 Liste des Clients" style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #333;" />
            <TableView fx:id="tableClients" prefHeight="200" prefWidth="700" style="-fx-background-color: transparent; -fx-border-color: #e0e0e0; -fx-border-radius: 5px;">
                <columns>
                    <TableColumn fx:id="colId" text="ID" prefWidth="50"/>
                    <TableColumn fx:id="colNom" text="Nom" prefWidth="200"/>
                    <TableColumn fx:id="colPrenom" text="Prénom" prefWidth="200"/>
                </columns>
            </TableView>
        </VBox>

        <VBox spacing="15" style="-fx-background-color: white; -fx-padding: 20px; -fx-background-radius: 10px; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.1), 5, 0, 0, 2);">
            <Label text="✏️ Ajouter/Modifier un Client" style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #333;" />
            <HBox alignment="CENTER_LEFT" spacing="15">
                <VBox spacing="5">
                    <Label style="-fx-font-size: 12px; -fx-text-fill: #666;" text="Nom" />
                    <TextField fx:id="nomField" prefWidth="200" promptText="Entrez le nom" style="-fx-background-color: #f8f9fa; -fx-border-color: #e0e0e0; -fx-border-radius: 5px; -fx-padding: 8px;"/>
                </VBox>
                <VBox spacing="5">
                    <Label style="-fx-font-size: 12px; -fx-text-fill: #666;" text="Prénom" />
                    <TextField fx:id="prenomField" prefWidth="200" promptText="Entrez le prénom" style="-fx-background-color: #f8f9fa; -fx-border-color: #e0e0e0; -fx-border-radius: 5px; -fx-padding: 8px;"/>
                </VBox>
            </HBox>

            <HBox spacing="10" styleClass="hbox-buttons">
                <Button text="➕ Ajouter" onAction="#addClient" styleClass="btn-primary" />
                <Button text="✏️ Modifier" onAction="#updateClient" styleClass="btn-secondary" />
                <Button text="🗑️ Supprimer" onAction="#deleteClient" styleClass="btn-danger" />
            </HBox>
        </VBox>
    </VBox>
</ScrollPane>
